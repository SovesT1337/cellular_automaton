# Обобщенный клеточный автомат

**Вычисление выходной последовательности обобщенного клеточного автомата по заданному графу и функции обратной связи.**

## Описание

Клеточный автомат - дискретная вычислительная модель, где каждая ячейка имеет состояние (0 или 1) и обновляется на основе состояний соседних ячеек согласно функции обратной связи.

**Обобщенный КА** отличается тем, что топология связей задается произвольным графом в формате CSR (Compressed Sparse Row).

## Теоретический анализ алгоритмов и модель вычислений

### 1. Постановка задачи

**Дано:**
- Граф G = (V, E), где |V| = N узлов, |E| = E рёбер
- Начальное состояние: S₀ ∈ {0,1}^N
- Функция обратной связи: f: {0,1}^k → {0,1}
- Количество шагов эволюции: T

**Требуется:** 
Вычислить состояние S_T после T шагов эволюции, где:
```
S_{t+1}[i] = f(S_t[neighbors(i)])
```

**Представление графа (CSR формат):**
- `row_ptr[N+1]` - указатели на начало списка соседей
- `col_idx[E]` - индексы соседей
- Соседи узла i: `col_idx[row_ptr[i] : row_ptr[i+1]]`
- Степень узла i: `deg(i) = row_ptr[i+1] - row_ptr[i]`

---

### 2. CPU алгоритм (последовательный)

#### Модель вычислений:

```
Последовательная обработка на CPU:
┌─────────────────────────────────────┐
│ Шаг t=1                             │
│  ├─ Узел 0: обработка deg(0) соседей│
│  ├─ Узел 1: обработка deg(1) соседей│
│  ├─ ...                             │
│  └─ Узел N-1: обработка deg(N-1)    │
├─────────────────────────────────────┤
│ Шаг t=2                             │
│  ├─ Узел 0 ...                      │
│  └─ ...                             │
└─────────────────────────────────────┘

Всего операций: T × Σ deg(i) = T × E
```

#### Алгоритмическая сложность CPU:

**Временная сложность:**
```
T_CPU = T × Σ(deg(i)) = T × E
         i=0..N-1

Обоснование:
  • Внешний цикл: T итераций (шаги эволюции)
  • Внутренний цикл: N итераций (узлы)
  • Для узла i: deg(i) операций (обработка соседей)
  • Сумма: Σ deg(i) = E (каждое ребро учитывается 1 раз)
         i=0..N-1

Итого: T_CPU = O(T × E)
```

**Для случайных графов Эрдёша-Реньи** со средней степенью d:
```
E = N × d  ⟹  T_CPU = O(T × N × d)
```

**Худший случай:** Полный граф (deg(i) = N-1 для всех i)
```
E = N(N-1)/2  ⟹  T_CPU = O(T × N²)
```

**Типичный случай:** Разреженный граф (deg(i) = O(1))
```
E = O(N)  ⟹  T_CPU = O(T × N)
```

---

### 3. CUDA алгоритм (параллельный)

#### Модель вычислений:

```
Параллельная обработка на GPU:

CPU (Host)                    GPU (Device)
─────────────────────────────────────────────────
Инициализация:
  ├─ Выделить память на GPU
  ├─ Копировать граф (row_ptr, col_idx): O(N+E)
  └─ Копировать S₀: O(N)
                                ↓
Цикл по шагам t=1..T:         Grid (N/256 блоков)
  ├─ Запуск kernel ────────→  ├─ Block 0 (256 потоков)
  │                            │  ├─ Thread 0 → Узел 0
  │                            │  ├─ Thread 1 → Узел 1
  │                            │  └─ ...
  │                            ├─ Block 1 (256 потоков)
  │                            │  ├─ Thread 0 → Узел 256
  │                            │  └─ ...
  │                            └─ ...
  │                            
  │  ОДНОВРЕМЕННО все N потоков:
  │  каждый обрабатывает deg(i) соседей
  │  
  ├─ Синхронизация            
  └─ Обмен буферов
                                ↓
Завершение:
  └─ Копировать S_T: O(N)

Параллелизм: N узлов обрабатываются одновременно
Время шага: max(deg(i)) вместо Σ deg(i)
            i=0..N-1              i=0..N-1
```

#### Алгоритмическая сложность CUDA:

**Временная сложность вычислений:**
```
T_compute = T × max(deg(i))
                i=0..N-1

Обоснование:
  • Все N узлов обрабатываются параллельно
  • Каждый поток i обрабатывает deg(i) соседей
  • Время определяется самым «тяжёлым» узлом: max(deg(i))
  • Внешний цикл: T итераций

Итого: T_compute = O(T × d_max)

где d_max = max deg(i) - максимальная степень узла
```

**Временная сложность передачи данных:**
```
T_transfer = T_init + T_final

где:
  T_init  = O(N + E) - копирование графа и начального состояния
  T_final = O(N)     - копирование конечного состояния
  
T_transfer = O(N + E)
```

**Полная временная сложность CUDA:**
```
T_CUDA = T_compute + T_transfer
       = O(T × d_max) + O(N + E)
```

**Для случайных графов** со средней степенью d:
```
d_max ≈ 2d (с высокой вероятностью)

T_CUDA = O(T × d) + O(N × d)
```

**Худший случай:** d_max = N-1
```
T_CUDA = O(T × N) + O(N²)
```

**Типичный случай:** d_max = O(1)
```
T_CUDA = O(T) + O(N)
```

---

### 4. Теоретическое ускорение (Speedup)

#### Определение ускорения:
```
Speedup(N, T) = T_CPU / T_CUDA
```

#### Расчёт для типичного случая:

**При больших T** (когда T >> N/d, передача данных пренебрежимо мала):
```
Speedup ≈ (T × E) / (T × d_max)
        = E / d_max
        = (N × d) / (2d)    [для случайных графов]
        = N / 2

Speedup = O(N)
```

**Полная формула с учётом передачи:**
```
Speedup(N, T) = (T × N × d) / (T × d_max + N × d)
              = (T × N × d) / (2T × d + N × d)    [для случайных графов]
              = (T × N) / (2T + N)
              
При T → ∞:  Speedup → N/2
При T → 0:  Speedup → 0  (доминирует T_transfer)
```

#### Критическое значение T:

CUDA эффективнее CPU когда Speedup > 1:
```
(T × N × d) / (2T × d + N × d) > 1

T × N × d > 2T × d + N × d

T × N > 2T + N

T(N - 2) > N

T > N / (N - 2)
```

**Для больших N:** T > 1 (практически всегда эффективно при T ≥ 10)

#### Зависимость от размера графа:

| Размер N | d_max | Speedup (теор.) | Speedup (эксп.) при T=100 |
|----------|-------|-----------------|----------------------------|
| 100      | ~10   | 100/(2×10) ≈ 5  | 0.01 (T_transfer >> T_compute) |
| 1,000    | ~10   | 1000/(2×10) ≈ 50 | 0.04 (T_transfer > T_compute) |
| 10,000   | ~10   | 10000/(2×10) ≈ 500 | 1.45 (T_transfer ≈ T_compute) |
| 100,000  | ~10   | 100000/(2×10) ≈ 5000 | 5.35 (ограничение bandwidth) |
| 250,000  | ~10   | 250000/(2×10) ≈ 12500 | 9.38 (ограничение bandwidth) |

---

### 5. Эффективность параллелизации

#### Определение эффективности:
```
Efficiency(N, p) = Speedup(N, p) / p

где p - количество процессоров (потоков)
```

#### Расчёт для CUDA:

**Количество потоков:** p = N (один поток на узел)

**Теоретическая эффективность:**
```
Efficiency = Speedup / N = (N/2) / N = 1/2 = 50%
```

**Интерпретация:**
- Каждый поток выполняет deg(i) операций
- Среднее: d операций на поток
- Максимум: d_max операций на самый загруженный поток
- Простой потоков: (d_max - deg(i)) операций
- Средний простой: (d_max - d) ≈ d
- Эффективность: d / d_max ≈ d / 2d = 50%

**Факторы снижения эффективности:**

1. **Неоднородность нагрузки** (load imbalance):
   ```
   Коэффициент неоднородности: σ_deg / d
   где σ_deg - стандартное отклонение степеней
   ```

2. **Латентность памяти:**
   - Глобальная память GPU: ~400 циклов
   - Кэш L2: ~200 циклов
   - Неоптимальный доступ снижает эффективность на 20-40%

3. **Накладные расходы синхронизации:**
   ```
   T_sync = O(число блоков) = O(N/256)
   ```

---

### 6. Условия эффективности CUDA

**Необходимое условие:** T_CUDA < T_CPU
```
T × d_max + (N + E) < T × E

T × d_max < T × E - (N + E)

T > (N + E) / (E - d_max)

T > (N + N×d) / (N×d - d_max)

T > (1 + d) / (d - d_max/N)
```

**Для случайных графов** (d_max ≈ 2d):
```
T > (1 + d) / (d - 2d/N)

При больших N:  T > (1 + d) / d ≈ 1

Практически: T ≥ 10 для гарантированного ускорения
```

**Зависимость от размера:**

| N | T_transfer (мс) | T_compute при T=100 (мс) | Speedup |
|---|-----------------|--------------------------|---------|
| 100 | ~7 | ~0.05 | 0.01x (transfer >> compute) |
| 1K | ~10 | ~0.4 | 0.04x (transfer > compute) |
| 10K | ~6 | ~9 | 1.45x (transfer ≈ compute) |
| 100K | ~21 | ~112 | 5.35x (compute > transfer) |
| 250K | ~30 | ~282 | 9.38x (compute >> transfer) |

**Критический размер графа:** N ≈ 10000 узлов
```
При N < 10000:  T_transfer доминирует → CPU быстрее
При N > 10000:  T_compute доминирует → CUDA быстрее
```

---

### 7. Сравнение CPU и CUDA

#### Идентичность входных данных:
✓ Один и тот же граф G = (V, E)  
✓ Одно и то же начальное состояние S₀  
✓ Одна и та же функция обратной связи f  
✓ Одинаковое количество шагов T  
✓ Проверка корректности: S_T^CPU = S_T^CUDA ✓

#### Источники ускорения на GPU:

1. **Массовый параллелизм:**
   - CPU: последовательная обработка N узлов
   - GPU: параллельная обработка N узлов
   - **Выигрыш:** O(N) → O(1) по узлам

2. **Эффективное использование памяти:**
   - Коалесцированный доступ к массивам
   - Высокая пропускная способность (bandwidth)

3. **SIMT архитектура:**
   - Single Instruction Multiple Threads
   - Эффективное выполнение одинаковых операций

#### Ограничения ускорения:

1. **Накладные расходы передачи:** O(N + E)
2. **Неоднородность графа:** время = max deg(i)
3. **Пропускная способность памяти:** bandwidth bottleneck
4. **Латентность глобальной памяти:** ~400 циклов

---

### 8. Выводы

**CPU алгоритм:**
```
T_CPU = O(T × E) = O(T × N × d)
```
- Эффективен для малых графов (N < 5000)
- Нет накладных расходов на передачу данных
- Простая реализация

**CUDA алгоритм:**
```
T_CUDA = O(T × d_max) + O(N + E)
```
- Эффективен для больших графов (N > 10000)
- Параллельная обработка: ускорение до O(N)
- Требует учёта передачи данных

**Теоретическое ускорение:**
```
Speedup = N / 2 (при T → ∞)
Efficiency = 50%
```

**Практические рекомендации:**
- N < 10000: использовать CPU
- N > 10000, T > 10: использовать CUDA
- Ожидаемое ускорение: 5-9x для N > 100K

## Краткое объяснение для презентации

### 📌 Что делает программа (простыми словами)

**Задача:** Моделирование клеточного автомата на графе.
- Есть граф из узлов (вершин), соединённых рёбрами
- Каждый узел имеет состояние: 0 или 1
- На каждом шаге узел смотрит на соседей и меняет своё состояние
- Нужно выполнить много таких шагов и получить итоговое состояние

**Пример:**
```
Узел №5 соединён с узлами 2, 7, 9
Их состояния: 1, 0, 1
Функция XOR: 1 ⊕ 0 ⊕ 1 = 0
Новое состояние узла №5 = 0
```

---

### 🖥️ Как работает CPU алгоритм

**Простое объяснение:**
CPU обрабатывает узлы **по очереди**, один за другим.

**Аналогия:** 
Как один человек моет посуду - берёт тарелку, моет, откладывает, берёт следующую.

**Процесс:**
1. Берём узел №0, смотрим на всех его соседей, считаем новое состояние
2. Берём узел №1, смотрим на всех его соседей, считаем новое состояние
3. И так далее для всех узлов...
4. Повторяем для следующего шага эволюции

**Время работы:**
- Для каждого узла нужно обработать его соседей
- Всего соседей у всех узлов = количество рёбер (E)
- Если шагов T, то время = T × E

**Формула:** `T_CPU = T × E`

**Что влияет на скорость:**
- Чем больше рёбер, тем дольше
- Чем больше шагов, тем дольше
- Обрабатывается последовательно (не параллельно)

---

### 🚀 Как работает GPU (CUDA) алгоритм

**Простое объяснение:**
GPU обрабатывает **все узлы одновременно**, параллельно.

**Аналогия:**
Как 1000 человек моют посуду одновременно - каждый берёт свою тарелку и все моют параллельно.

**Процесс:**
1. Отправляем весь граф на GPU (видеокарту)
2. Запускаем сразу N потоков (по одному на каждый узел)
3. Каждый поток независимо обрабатывает свой узел
4. Все узлы обрабатываются **одновременно**!
5. Повторяем для следующего шага

**Время работы:**
- Все узлы обрабатываются одновременно
- Время определяется самым "тяжёлым" узлом (у которого больше всего соседей)
- Плюс время на передачу данных CPU ↔ GPU

**Формула:** `T_CUDA = T × max_degree + T_transfer`

где:
- `max_degree` - максимальное количество соседей у одного узла
- `T_transfer` - время копирования данных между CPU и GPU

**Что влияет на скорость:**
- Не зависит от общего количества узлов (они параллельны!)
- Зависит от самого "связанного" узла
- Есть накладные расходы на передачу данных

---

### ⚡ Почему GPU быстрее (объяснение ускорения)

**Ключевая идея:**
CPU обрабатывает N узлов последовательно → время N  
GPU обрабатывает N узлов параллельно → время 1  
**Ускорение ≈ N раз!**

**Формула ускорения:**
```
Speedup = Время_CPU / Время_GPU ≈ N/2
```

**Почему именно N/2, а не N?**
- Узлы имеют разное количество соседей
- GPU ждёт, пока самый "тяжёлый" узел закончит
- Половина времени тратится на ожидание
- Эффективность = 50%

**Наглядный пример:**

| Размер графа | CPU время | GPU время | Ускорение |
|--------------|-----------|-----------|-----------|
| 100 узлов    | 0.05 мс   | 7.0 мс    | **0.007x** (GPU медленнее!) |
| 1,000 узлов  | 0.44 мс   | 9.9 мс    | **0.044x** (GPU медленнее) |
| 10,000 узлов | 9.1 мс    | 6.3 мс    | **1.4x** (примерно равно) |
| 100,000 узлов| 112 мс    | 20.9 мс   | **5.3x** (GPU быстрее!) |
| 250,000 узлов| 282 мс    | 30.1 мс   | **9.4x** (GPU намного быстрее!) |

---

### 📊 Когда использовать CPU, а когда GPU

**Правило большого пальца:** Граница ≈ 10,000 узлов

#### Малые графы (< 10,000 узлов)
**Используйте CPU!**

**Почему:**
- Времени на вычисления мало
- Времени на передачу данных GPU много
- Накладные расходы "съедают" весь выигрыш

**Аналогия:** 
Не нанимайте 1000 человек, чтобы помыть 10 тарелок - 
один человек справится быстрее, чем вы их всех соберёте.

#### Большие графы (> 10,000 узлов)
**Используйте GPU!**

**Почему:**
- Времени на вычисления много
- Времени на передачу данных относительно мало
- Параллелизм даёт огромный выигрыш

**Аналогия:**
Если нужно помыть 100,000 тарелок - лучше нанять 1000 человек,
один человек будет мыть слишком долго.

---

### 🎯 Основные тезисы для презентации

**1. Что реализовано:**
- Клеточный автомат на произвольном графе
- Две реализации: CPU (последовательная) и GPU (параллельная)
- 61 тест на графах от 50 до 300,000 узлов

**2. Теоретические результаты:**
- CPU: время = T × E (линейно от количества рёбер)
- GPU: время = T × max_degree (не зависит от числа узлов!)
- Ускорение: до N/2 раз (теоретически)

**3. Практические результаты:**
- Малые графы (<10K): CPU быстрее из-за накладных расходов
- Большие графы (>10K): GPU быстрее в 5-9 раз
- Максимальное ускорение: 9.38x на графе 250,000 узлов

**4. Почему GPU быстрее:**
- Массовый параллелизм: все узлы обрабатываются одновременно
- GPU имеет тысячи ядер для параллельных вычислений
- Эффективен для регулярных операций на больших данных

**5. Ограничения GPU:**
- Накладные расходы на передачу данных
- Эффективность 50% из-за неравномерной нагрузки
- Требует специального оборудования (видеокарта NVIDIA)

---

### 📈 Графики для презентации

**График 1: Время выполнения от размера графа**
- Ось X: Количество узлов (логарифмическая шкала)
- Ось Y: Время в миллисекундах (логарифмическая шкала)
- Две линии: CPU (синяя) и CUDA (красная)
- **Вывод:** На малых графах CPU быстрее, на больших - GPU

**График 2: Ускорение от размера графа**
- Ось X: Количество узлов
- Ось Y: Ускорение (раз)
- Горизонтальная линия на y=1 (паритет)
- **Вывод:** Ускорение растёт с размером графа

**Что показать:**
1. Точка пересечения (~10000 узлов) - где GPU становится быстрее
2. Максимальное ускорение (9.38x на 250K узлов)
3. Зону неэффективности GPU (<10K узлов)

---

### 💡 Часто задаваемые вопросы

**Q: Почему GPU не всегда быстрее?**  
A: Есть накладные расходы на передачу данных между CPU и GPU. 
На малых задачах эти расходы больше, чем выигрыш от параллелизма.

**Q: Почему ускорение не линейное (не 100x, не 1000x)?**  
A: 
1. Неравномерная нагрузка - узлы имеют разное число соседей
2. Ограничение памяти GPU
3. Время передачи данных
4. Реальная эффективность ≈ 50%

**Q: Для каких задач подходит этот подход?**  
A: Для любых задач, где нужно:
- Обработать граф с большим числом узлов
- Выполнить одинаковые операции на всех узлах
- Многократно обновлять состояние (много шагов)

**Примеры:** моделирование сетей, анализ социальных графов, 
распространение информации, физические симуляции.

**Q: Что нужно для запуска?**  
A: 
- CPU версия: любой компьютер с C++17
- GPU версия: видеокарта NVIDIA + CUDA Toolkit

---

### 📝 Шаблон слайда для презентации

```
┌─────────────────────────────────────────┐
│  Сравнение CPU и GPU реализаций         │
├─────────────────────────────────────────┤
│                                         │
│  CPU алгоритм (последовательный)        │
│  • Обработка узлов по очереди           │
│  • Время: T × E                         │
│  • Эффективен для малых графов          │
│                                         │
│  GPU алгоритм (параллельный)            │
│  • Обработка всех узлов одновременно    │
│  • Время: T × max_degree                │
│  • Эффективен для больших графов        │
│                                         │
│  Результат:                             │
│  • Ускорение до 9.38x на графе 250K узлов│
│  • Порог эффективности: 10,000 узлов    │
└─────────────────────────────────────────┘
```

## Структура проекта

```
cellular_automaton/
├── cellular_automaton.h        # Основные структуры и API
├── cpu_automaton.cpp           # CPU реализация
├── cuda_automaton.cu           # CUDA реализация (GPU)
├── main.cpp                    # Бенчмарк и сравнение производительности
├── plot_benchmarks.py          # Скрипт для построения графиков
├── Makefile                    # Сборка проекта
├── README.md                   # Документация
├── benchmark_results.csv       # Результаты (генерируется при запуске)
└── benchmark_plots.png         # Графики (генерируются скриптом)
```

## Сборка и запуск

### С CUDA (требуется NVIDIA GPU и CUDA Toolkit)
```bash
make
./cellular_automaton
```

### Только CPU
```bash
make cpu
./cellular_automaton_cpu
```

### Построение графиков
```bash
# После запуска бенчмарков
python3 plot_benchmarks.py
# или
./plot_benchmarks.py
```

## Тестирование производительности

Программа выполняет **61 бенчмарк** с различными параметрами:

### 📊 Набор 1: Влияние размера графа (XOR функция, 41 тест)
- **Очень малые графы:** 50, 75, 100, 150, 200, 250, 300, 400, 500 узлов (9 тестов)
- **Малые графы:** 600, 800, 1K, 1.5K, 2K, 2.5K, 3K, 4K, 5K узлов (9 тестов)
- **Средние графы:** 6K, 7K, 8K, 9K, 10K, 12K, 15K, 18K, 20K узлов (9 тестов)
- **Большие графы:** 25K, 30K, 40K, 50K, 60K, 70K, 80K, 90K, 100K узлов (9 тестов)
- **Очень большие графы:** 120K, 150K, 200K, 250K, 300K узлов (5 тестов)

### 📊 Набор 2: Функция MAJORITY (20 тестов)
- Полный диапазон: 100, 200, 500, 750, 1K, 1.5K, 2K, 3K, 5K, 7K, 10K, 15K, 20K, 30K, 50K, 70K, 100K, 150K, 200K, 300K узлов

**Параметры тестирования:**
- Средняя степень графа: 5 соседей на узел
- Количество шагов эволюции: 100
- Seed для генерации: фиксированный (42)

## Результаты производительности

### Ключевые выводы из бенчмарков:

| Параметр | Описание | Результат |
|----------|----------|-----------|
| **Пороговый размер** | Граф, начиная с которого CUDA быстрее | **~10,000 узлов** |
| **Максимальное ускорение** | Лучший результат CUDA | **9.38x** (250K узлов, XOR) |
| **Малые графы (<1K)** | Накладные расходы GPU | CPU быстрее в 20-200x |
| **Средние графы (1K-10K)** | Переходная зона | Производительность сравнима |
| **Большие графы (10K-100K)** | Эффективный параллелизм | CUDA быстрее в 1.4-5.3x |
| **Очень большие графы (>100K)** | Максимальная эффективность | CUDA быстрее в 5-9x |

### Примеры времени выполнения (XOR функция, 100 шагов):

```
┌──────────┬────────────┬────────────┬───────────┐
│  Узлов   │  CPU (мс)  │ CUDA (мс)  │ Ускорение │
├──────────┼────────────┼────────────┼───────────┤
│      100 │      0.052 │      7.027 │     0.01x │
│    1,000 │      0.437 │      9.923 │     0.04x │
│   10,000 │      9.106 │      6.281 │     1.45x │
│   50,000 │     48.927 │     10.395 │     4.71x │
│  100,000 │    111.698 │     20.889 │     5.35x │
│  200,000 │    199.189 │     26.517 │     7.51x │
│  250,000 │    282.120 │     30.072 │     9.38x │
│  300,000 │    307.841 │     35.184 │     8.75x │
└──────────┴────────────┴────────────┴───────────┘
```

## 📈 Выходные данные

### CSV файл: `benchmark_results.csv`

Содержит полные результаты всех тестов:

```csv
num_nodes,num_edges,avg_degree,steps,feedback_type,feedback_name,cpu_time_ms,cuda_time_ms,speedup,results_match
50,248,4.96,100,0,XOR,0.028200,11.552896,0.002441,true
100,510,5.10,100,0,XOR,0.047900,10.187904,0.004702,true
...
```

**Колонки:**
- `num_nodes` - количество узлов в графе
- `num_edges` - количество рёбер в графе
- `avg_degree` - средняя степень узла
- `steps` - количество шагов эволюции
- `feedback_type` - тип функции (0=XOR, 1=MAJORITY)
- `feedback_name` - название функции
- `cpu_time_ms` - время CPU в миллисекундах
- `cuda_time_ms` - время CUDA в миллисекундах
- `speedup` - ускорение (cpu_time / cuda_time)
- `results_match` - совпадение результатов CPU и CUDA

### Графики: `benchmark_plots.png`

Скрипт `plot_benchmarks.py` генерирует 2 графика:

1. **Функция XOR**: Сравнение времени выполнения CPU vs CUDA от размера графа (линейные оси)
2. **Функция MAJORITY**: Сравнение времени выполнения CPU vs CUDA от размера графа (линейные оси)

Оба графика показывают зависимость времени выполнения от количества узлов в графе при 100 шагах эволюции.

**Особенности графиков:**
- Линейные оси для наглядности
- Диапазон: от 50 до 300,000 узлов
- Тонкие линии для презентации
- Отчетливо видно линейный рост CPU и почти константное время CUDA

## API

### Основные структуры

```cpp
// Граф в CSR формате
struct Graph {
    std::vector<int> row_ptr;    // Размер: num_nodes + 1
    std::vector<int> col_idx;    // Размер: num_edges
    int num_nodes;
    int num_edges;
};

// Конфигурация выходной последовательности
struct OutputConfig {
    std::vector<int> cells;           // Индексы ячеек для извлечения
    int extract_every_n_steps;        // Извлекать каждые N шагов
};

// Результат вычисления
struct ComputeResult {
    std::vector<uint8_t> final_state;
    std::vector<std::vector<uint8_t>> history;
    std::vector<uint8_t> output_sequence;
    double elapsed_ms;
};
```

### Генерация графа

```cpp
// Случайный граф Эрдёша-Реньи
Graph generate_random_graph(int num_nodes, int avg_degree, unsigned int seed);
```

### Функции обратной связи

```cpp
// XOR всех соседей (тип 0)
uint8_t feedback_xor(const uint8_t* neighbors, int count);

// Majority - большинство голосов (тип 1)
uint8_t feedback_majority(const uint8_t* neighbors, int count);
```

### Вычисления

```cpp
// CPU реализация
ComputeResult compute_cpu(
    const Graph& graph, 
    const std::vector<uint8_t>& initial_state,
    int steps, 
    int feedback_type,  // 0=XOR, 1=Majority
    const OutputConfig* output_cfg = nullptr
);

// CUDA реализация (GPU)
ComputeResult compute_cuda(
    const Graph& graph,
    const std::vector<uint8_t>& initial_state,
    int steps,
    int feedback_type,
    const OutputConfig* output_cfg = nullptr
);
```

## Пример использования

```cpp
#include "cellular_automaton.h"
#include <iostream>

int main() {
    // Создание случайного графа
    Graph graph = generate_random_graph(1000, 5, 42);
    
    // Случайное начальное состояние
    std::vector<uint8_t> initial(1000);
    for (int i = 0; i < 1000; i++) {
        initial[i] = rand() % 2;
    }
    
    // Конфигурация выходной последовательности
    OutputConfig output_cfg;
    output_cfg.cells = {0, 1, 2, 3, 4};
    output_cfg.extract_every_n_steps = 1;
    
    // Вычисление на CPU
    auto result_cpu = compute_cpu(graph, initial, 100, 0, &output_cfg);
    std::cout << "CPU время: " << result_cpu.elapsed_ms << " мс\n";
    
    // Вычисление на CUDA
    auto result_cuda = compute_cuda(graph, initial, 100, 0, &output_cfg);
    std::cout << "CUDA время: " << result_cuda.elapsed_ms << " мс\n";
    
    // Проверка корректности
    bool match = (result_cpu.output_sequence == result_cuda.output_sequence);
    std::cout << "Совпадение: " << (match ? "✓" : "✗") << "\n";
    
    return 0;
}
```

## Формат графа (CSR)

Compressed Sparse Row - эффективный формат для GPU:

```
Граф: 0→1, 0→2, 1→2, 2→0

row_ptr = [0, 2, 3, 4]   # Начало списка соседей
col_idx = [1, 2, 2, 0]   # Индексы соседей

Соседи вершины i: col_idx[row_ptr[i] : row_ptr[i+1]]
```

## Требования

### Обязательные
- C++17
- g++ или clang++

### Опциональные (для CUDA версии)
- CUDA Toolkit 11.0+
- NVIDIA GPU с compute capability 3.5+

### Для построения графиков
- Python 3.x
- matplotlib

```bash
pip install matplotlib
```

## Очистка

```bash
make clean  # Удалить скомпилированные файлы
```
